const card__cityBrazil = document.querySelectorAll('.--brazil .card__city-country');
const card__priceText = document.querySelectorAll('.card__price-text');

card__cityBrazil.forEach(textfield => textfield.textContent = "Brasil");
card__priceText.forEach(textfield => textfield.textContent = "A partir de");